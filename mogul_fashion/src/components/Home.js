import React from 'react';

const Home = () => {
    return (
        <div>
        <header className="topSection">
        <nav>

        </nav>

        <button>

        </button>

        <button></button>

        </header>
        
        </div>
      );
}

export default Home;