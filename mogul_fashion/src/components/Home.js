import React from 'react';
import './Home.scss';

const Home = () => {
    return (
        <div>
        <header className="topSection">
        <nav className="hamburger">

        </nav>

        <button>

        </button>

        <button></button>

        </header>
        
        </div>
      );
}

export default Home;