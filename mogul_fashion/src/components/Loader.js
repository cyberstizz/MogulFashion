import React from 'react';
import './Loader.scss'

const Loader = () => {
    return (
        <div className="loader">
            <img src="/Loading_2.gif" alt="Loading..." />
        </div>
    );
}

export default Loader;
